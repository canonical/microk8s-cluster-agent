name: cla-check

on:
  pull_request:
    branches: [1.34]

permissions:
  contents: read
   
jobs:
  cla-check:
    runs-on: ubuntu-latest
    steps:
      - name: Check if CLA signed
        uses: canonical/has-signed-canonical-cla@v2
